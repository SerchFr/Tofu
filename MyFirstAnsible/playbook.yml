---
- name: Configurer nginx sur la VM Infomaniak
  hosts: webserver
  become: true
  #remote_user: root
  
  roles:
    - nginx
    
  #tasks:
  #- name: Ensure apache is at the latest version
  #  ansible.builtin.yum:
  #    name: httpd
  #    state: latest
